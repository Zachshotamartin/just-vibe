# v0.11.0 validation

The release adds reprompt and short invocations, plus the runtime corrections documented in the two September 21 code reviews. The catalog contains 220 skill names, 217 canonical workflows and 112 profiles.

## Invocation checks

Claude Code 2.1.258 discovered all 220 jv aliases and all 220 canonical just-vibe commands. Four live requests exercised the two dispatchers and the two colon namespaces. Each returned a rewritten prompt; the three forwarding paths invoked the canonical reprompt skill with the complete arguments.

Codex 0.152.0 completed four live CLI requests containing the same forms through an isolated installation and trusted prompt hooks. Each selected reprompt and loaded its instructions. This verifies prompt-text handling, not custom slash-menu support. Use Codex's skill picker where its input box reserves slash syntax. One Codex run emitted extra tracking commentary; the tests do not establish perfect adherence or broad prompt quality.

Both hosts passed isolated native setup, update, doctor, uninstall and reinstall lifecycles with v0.11.0 metadata. The tests did not change normal host configuration or install trust globally. Other editor invocation syntax was not live-tested.

## Regression and packaging

The implementation regression run passed 399 of 400 tests; the remaining Windows-only case was skipped on macOS. Fixtures cover all catalog IDs through all four parser forms, exact argument preservation, unknown names, selective installations, conflict preflight and user-edit preservation. Website generation produced 355 pages and all four static tests passed.

The publication procedure reruns release checks and tests the exact archive through npm, pnpm 10/12 and Yarn 4 before upload. Registry integrity, source commit, hashes and the results of that procedure belong in the separate publication record after they are observed.

## Limits

GitHub Actions cannot start for this private repository because of account billing restrictions. The maintainer authorized release using local checks while that restriction remains. No CI success, Linux/Windows execution, every-command behavioral coverage or universal editor support is claimed. The normal publisher's CI gate remains intact; any manual publication exception applies only to this release.
